metadata:
  feature: "cli-interface"
  description: "F5: CLI 接口 — 面向 AI agent 的命令行界面"
  generated_at: "2026-04-06T16:30:00+08:00"

stories:
  - name: "完整交互流程"
    acceptance_criteria:
      - "agent 通过 CLI 完成'打开页面 → 查看元素 → 点击 → 填写 → 截图'流程"
      - "每个命令响应时间 < 500ms（daemon 已启动，不含网络延迟）"
      - "输出为结构化纯文本，agent 可直接解析"

  - name: "错误处理与诊断"
    acceptance_criteria:
      - "命令失败时返回非零退出码"
      - "错误信息包含足够上下文让 agent 自主诊断和重试"
      - "错误输出到 stderr，正常输出到 stdout"

  - name: "小红书端到端发帖"
    acceptance_criteria:
      - "通过 CLI 命令完成小红书图文发帖全流程（导航 → 上传图片 → 填标题 → 填正文 → 添加标签 → 发布）"
      - "不触发反爬检测"
      - "不需要人工介入（Cookie 有效的前提下）"

  - name: "Twitter 端到端发推"
    acceptance_criteria:
      - "通过 CLI 命令完成 Twitter 发推全流程（导航首页 → 写推文 → 发布）"
      - "支持 thread（多条推文串联）"
      - "不触发反爬检测"
      - "不需要人工介入（Cookie 有效的前提下）"
